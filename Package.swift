// swift-tools-version:5.5

import PackageDescription

let package = Package(
    name: "SliderKit",
    products: [
        .library(
            name: "SliderKit",
            targets: ["SliderKit"]),
    ],
    targets: [
        .target(
            name: "SliderKit",
            dependencies: [])
    ]
)
